import styled from 'styled-components';

export const Wrapper = styled.div`
  position: fixed;
  bottom: 20px;
  left: 20px;
`;

export const Img = styled.img`
  width: 20rem;
  z-index: 5;
  cursor: pointer;
  position: relative;
`;
